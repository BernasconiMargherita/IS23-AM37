package it.polimi.ingsw.controller;

public class initMessage extends Message{
    public initMessage(int gameID, String message, String senderNickname) {
        super(gameID, message, senderNickname);
    }
}
